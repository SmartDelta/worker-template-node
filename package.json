{
  "name": "Worker",
  "version": "1.0.0",
  "description": "A worker example for node js.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon",
    "pkg": "rm -rf dist/ && pkg package.json && cp ./config.json dist/config.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": "./index.js",
  "pkg": {
    "assets": [
      "config.json"
    ],
    "targets": [
      "node16-win-x64"
    ],
    "outputPath": "dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.14",
    "socket.io-client": "^2.3.0"
  }
}
